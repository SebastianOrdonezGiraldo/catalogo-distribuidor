PORT=3000
DOCUMENTS_DIR=./storage/documents
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
ADMIN_SESSION_SECRET=change_this_secret
# Solo la primera vez se copian los PDF del repo a DOCUMENTS_DIR. Tras eso, un borrado no vuelve al reiniciar.
# Para forzar otra copia desde el repo: DOCUMENTS_FORCE_RESEED=true (una vez) y reinicia.
#DOCUMENTS_FORCE_RESEED=false
